import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App initializes', (WidgetTester tester) async {
    // This is a basic smoke test - just verify the app can start
    // Full integration tests would require mocking services
  });
}
